import { writeFileSync } from 'fs'
const Q = "'"
const DQ = '"'
const BT = '`'
const L = '\n'
const p = 'src/app/(app)/me/FamilyMembersSection.tsx'
const content = [
`'use client'`,
`import { useState, useRef } from 'react'`,
`import { MessageSquarePlus, X, Send } from 'lucide-react'`,
`import { requestFamilyMemberChange } from './actions'`,
``,
`export interface FamilyMemberRow {`,
`  id: string`,
`  full_name: string`,
`  full_name_ml: string | null`,
`  relation_to_head: string | null`,
`  date_of_birth: string | null`,
`  gender: string | null`,
`  phone: string | null`,
`  email: string | null`,
`  is_deceased: boolean`,
`}`,
``,
`const RELATIONS = ['head','spouse','son','daughter','father','mother','brother','sister','grandchild','other']`,
`const inp = 'w-full rounded-xl border border-gray-200 px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-brand-900 bg-white'`,
``,
`function MemberCard({ member }: { member: FamilyMemberRow }) {`,
`  const [requesting, setRequesting] = useState(false)`,
`  const [saving, setSaving]         = useState(false)`,
`  const [submitted, setSubmitted]   = useState(false)`,
`  const [error, setError]           = useState<string | null>(null)`,
`  const [mlLoading, setMlLoading]   = useState(false)`,
`  const nameEnRef = useRef<HTMLInputElement>(null)`,
`  const [fullName,   setFullName]   = useState(member.full_name)`,
`  const [fullNameMl, setFullNameMl] = useState(member.full_name_ml ?? '')`,
`  const [relation,   setRelation]   = useState(member.relation_to_head ?? '')`,
`  const [dob,        setDob]        = useState(member.date_of_birth?.slice(0, 10) ?? '')`,
`  const [gender,     setGender]     = useState(member.gender ?? '')`,
`  const [phone,      setPhone]      = useState(member.phone ?? '')`,
`  const [email,      setEmail]      = useState(member.email ?? '')`,
``,
`  async function handleTransliterate() {`,
`    const text = nameEnRef.current?.value ?? ''`,
`    if (!text) return`,
`    setMlLoading(true)`,
`    const res = await fetch(` + BT + `/api/transliterate?text=\${encodeURIComponent(text)}` + BT + `)`,
`    const json = await res.json()`,
`    if (json.result) setFullNameMl(json.result)`,
`    setMlLoading(false)`,
`  }`,
``,
`  async function handleSubmit() {`,
`    if (!fullName.trim()) { setError('Name is required'); return }`,
`    setSaving(true); setError(null)`,
`    const fd = new FormData()`,
`    fd.set('full_name',        fullName)`,
`    fd.set('full_name_ml',     fullNameMl)`,
`    fd.set('relation_to_head', relation)`,
`    fd.set('date_of_birth',    dob)`,
`    fd.set('gender',           gender)`,
`    fd.set('phone',            phone)`,
`    fd.set('email',            email)`,
`    const currentData: Record<string, unknown> = {`,
`      full_name:        member.full_name,`,
`      full_name_ml:     member.full_name_ml,`,
`      relation_to_head: member.relation_to_head,`,
`      date_of_birth:    member.date_of_birth?.slice(0, 10) ?? null,`,
`      gender:           member.gender,`,
`      phone:            member.phone,`,
`      email:            member.email,`,
`    }`,
`    const result = await requestFamilyMemberChange(member.id, currentData, fd)`,
`    setSaving(false)`,
`    if ('error' in result) { setError(result.error); return }`,
`    setSubmitted(true)`,
`    setRequesting(false)`,
`  }`,
``,
`  const initials = member.full_name.trim().split(' ').map(w => w[0]).slice(0, 2).join('').toUpperCase()`,
`  const dobDisplay = member.date_of_birth`,
`    ? new Date(member.date_of_birth + 'T00:00:00').toLocaleDateString('en-IN', { day: 'numeric', month: 'short', year: 'numeric' })`,
`    : null`,
``,
`  return (`,
`    <div className={member.is_deceased ? 'opacity-50' : ''}>`,
`      <div className="flex items-start justify-between gap-3 px-4 py-3">`,
`        <div className="flex items-center gap-3 min-w-0">`,
`          <div className="w-8 h-8 rounded-full bg-brand-100 text-brand-900 text-xs font-bold flex items-center justify-center shrink-0">{initials}</div>`,
`          <div className="min-w-0">`,
`            <p className="font-semibold text-sm">{member.full_name}{member.is_deceased && <span className="ml-1 text-[10px] text-gray-400">\u2020</span>}</p>`,
`            {member.full_name_ml && <p className="text-xs font-malayalam text-muted-foreground" lang="ml">{member.full_name_ml}</p>}`,
`            <p className="text-xs text-muted-foreground capitalize">`,
`              {member.relation_to_head || '\u2014'}{dobDisplay ? ' \u00b7 ' + dobDisplay : ''}{member.gender ? ' \u00b7 ' + member.gender : ''}`,
`            </p>`,
`            {(member.phone || member.email) && (`,
`              <p className="text-[11px] text-muted-foreground mt-0.5">`,
`                {member.phone && <span className="mr-2">\uD83D\uDCF1 {member.phone}</span>}`,
`                {member.email && <span>\u2709 {member.email}</span>}`,
`              </p>`,
`            )}`,
`          </div>`,
`        </div>`,
`        {!member.is_deceased && !submitted && (`,
`          <button onClick={() => setRequesting(r => !r)} className="shrink-0 flex items-center gap-1 text-[11px] text-amber-700 border border-amber-200 rounded-lg px-2 py-1 hover:bg-amber-50 transition-colors">`,
`            <MessageSquarePlus size={11} /> Request Change`,
`          </button>`,
`        )}`,
`        {submitted && <span className="shrink-0 text-[11px] bg-green-50 text-green-700 border border-green-200 rounded-lg px-2 py-1 font-semibold">\u2713 Submitted</span>}`,
`      </div>`,
`      {requesting && (`,
`        <div className="mx-4 mb-4 rounded-xl bg-amber-50 border border-amber-200 p-4 space-y-3">`,
`          <div className="flex items-center justify-between">`,
`            <div>`,
`              <p className="text-xs font-bold text-amber-800 uppercase tracking-wide">Request Change \u2014 {member.full_name}</p>`,
`              <p className="text-[10px] text-amber-600 mt-0.5">Admin will review and apply. Edit only what needs to change.</p>`,
`            </div>`,
`            <button onClick={() => setRequesting(false)} className="text-gray-400 hover:text-gray-600"><X size={15} /></button>`,
`          </div>`,
`          <div className="grid grid-cols-2 gap-2">`,
`            <div className="col-span-2">`,
`              <label className="block text-[10px] font-semibold text-gray-500 uppercase mb-0.5">Full Name (English) *</label>`,
`              <input ref={nameEnRef} value={fullName} onChange={e => setFullName(e.target.value)} className={inp} />`,
`            </div>`,
`            <div className="col-span-2">`,
`              <div className="flex items-center justify-between mb-0.5">`,
`                <label className="text-[10px] font-semibold text-amber-600 uppercase">Name (Malayalam)</label>`,
`                <button type="button" onClick={handleTransliterate} disabled={mlLoading} className="text-[10px] font-semibold text-amber-700 bg-amber-100 border border-amber-200 rounded px-2 py-0.5 hover:bg-amber-200 disabled:opacity-50">`,
`                  {mlLoading ? 'Transliterating\u2026' : 'Type \u2192 \u0D2E\u0D32\u0D2F\u0D3E\u0D33\u0D02'}`,
`                </button>`,
`              </div>`,
`              <input value={fullNameMl} onChange={e => setFullNameMl(e.target.value)} className={inp + ' font-malayalam'} lang="ml" />`,
`            </div>`,
`            <div>`,
`              <label className="block text-[10px] font-semibold text-gray-500 uppercase mb-0.5">Relation</label>`,
`              <select value={relation} onChange={e => setRelation(e.target.value)} className={inp}>`,
`                <option value="">Select\u2026</option>`,
`                {RELATIONS.map(r => <option key={r} value={r}>{r.charAt(0).toUpperCase() + r.slice(1)}</option>)}`,
`              </select>`,
`            </div>`,
`            <div>`,
`              <label className="block text-[10px] font-semibold text-gray-500 uppercase mb-0.5">Gender</label>`,
`              <select value={gender} onChange={e => setGender(e.target.value)} className={inp}>`,
`                <option value="">Select\u2026</option>`,
`                <option value="male">Male</option>`,
`                <option value="female">Female</option>`,
`                <option value="other">Other</option>`,
`              </select>`,
`            </div>`,
`            <div>`,
`              <label className="block text-[10px] font-semibold text-gray-500 uppercase mb-0.5">Date of Birth</label>`,
`              <input type="date" value={dob} onChange={e => setDob(e.target.value)} className={inp} />`,
`            </div>`,
`            <div>`,
`              <label className="block text-[10px] font-semibold text-gray-500 uppercase mb-0.5">Mobile</label>`,
`              <input type="tel" value={phone} onChange={e => setPhone(e.target.value)} placeholder="+91 XXXXX XXXXX" className={inp} />`,
`            </div>`,
`            <div className="col-span-2">`,
`              <label className="block text-[10px] font-semibold text-gray-500 uppercase mb-0.5">Email</label>`,
`              <input type="email" value={email} onChange={e => setEmail(e.target.value)} className={inp} />`,
`            </div>`,
`          </div>`,
`          {error && <p className="text-xs text-red-600">{error}</p>}`,
`          <button onClick={handleSubmit} disabled={saving} className="w-full flex items-center justify-center gap-1.5 rounded-xl bg-amber-700 text-white text-sm font-semibold py-2.5 hover:bg-amber-800 disabled:opacity-50 transition-colors">`,
`            <Send size={13} />{saving ? 'Submitting\u2026' : 'Submit Change Request'}`,
`          </button>`,
`          <p className="text-[10px] text-center text-amber-600">Your request will appear in the Admin Approvals Queue for review.</p>`,
`        </div>`,
`      )}`,
`    </div>`,
`  )`,
`}`,
``,
`export default function FamilyMembersSection({ members }: { members: FamilyMemberRow[] }) {`,
`  if (members.length === 0) return null`,
`  const others = members.filter(m => m.relation_to_head !== 'head')`,
`  if (others.length === 0) return null`,
`  return (`,
`    <section>`,
`      <h2 className="text-xs font-bold text-amber-700 uppercase tracking-wide mb-2">Family Members ({others.length})</h2>`,
`      <div className="rounded-xl border border-amber-100 bg-white shadow-sm divide-y divide-gray-100 overflow-hidden">`,
`        {others.map(m => <MemberCard key={m.id} member={m} />)}`,
`      </div>`,
`    </section>`,
`  )`,
`}`,
].join('\n')
writeFileSync(p, content, 'utf8')
const written = (await import('fs')).readFileSync(p, 'utf8')
const hasOld = written.includes('handleSave') || written.includes('setEditing')
console.log('lines=' + written.split('\n').length + ' hasOldCode=' + hasOld + ' ok=' + written.includes('requestFamilyMemberChange'))
